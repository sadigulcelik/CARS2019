import java.util.*;
public class TimesSquare extends Intersection{  
   /* Effectively creates a new Grid by creating the initial intersection
    * @param length the "distance" it takes to cross the intersection
    * @param x the x coordinate of the intersection
    * @param y the y coordinate of the intersection
    */
    public TimesSquare(double x,double y,double length){
        super(length,x,y);
    }
}